# Local dev. On Oracle A1 replace :80 with the hostname and Caddy gets certs itself.
:80 {
	encode gzip

	handle /api/* {
		uri strip_prefix /api
		reverse_proxy api:8000
	}

	handle {
		reverse_proxy api:8000
	}
}
